set(EXTLIB_NAME Eigen)
set(EXTLIB_TGZ ${CMAKE_SOURCE_DIR}/external/${EXTLIB_NAME}.tar.gz)
set(EXTLIB_DIR ${CMAKE_BINARY_DIR}/include/${EXTLIB_NAME})

if(NOT EXISTS ${EXTLIB_DIR})
  execute_process(COMMAND ${CMAKE_COMMAND} -E tar xzf
    ${EXTLIB_TGZ} WORKING_DIRECTORY ${CMAKE_BINARY_DIR}/include)
endif(NOT EXISTS ${EXTLIB_DIR})

